{
  "name": "cartside-blitz",
  "version": "0.1.0",
  "private": true,
  "description": "Gentle shopping-budget Chrome side panel with Mom + Bestie OpenRouter agents.",
  "scripts": {
    "dev": "node --watch backend/server.js",
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check backend/agents.js && node --check extension/service-worker.js && node --check extension/content.js && node --check extension/sidepanel.js && node --check extension/options.js",
    "test": "node --test backend/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
